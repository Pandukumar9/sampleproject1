import { CustamdirectiveDirective } from './custamdirective.directive';

describe('CustamdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustamdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

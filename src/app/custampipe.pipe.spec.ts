import { CustampipePipe } from './custampipe.pipe';

describe('CustampipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustampipePipe();
    expect(pipe).toBeTruthy();
  });
});
